import React from "react";
import ParallaxLayer from "react-spring/dist/addons";

const ImagePage = ({ image, header }) => {
  return (
    <React.Fragment>
      <ParallaxLayer>sdads</ParallaxLayer>
    </React.Fragment>
  );
};

export default ImagePage;
